var text = "PRESS P TO START GAME";
var initialSlot = 12;
var jumpLines = 12;

var slotsInLine = 42;
var relativeSlot = initialSlot + (slotsInLine * jumpLines)

var output = "";

for(var i = 0; i < text.length; i++){
	output += "\n\t\tpushInStack($ra)\n";
	output += "\t\tsendParameters(" + (relativeSlot++) + ", " + text[i].charCodeAt(0) + ")\n";
	output += "\t\tjal drawLetters\n"
	output += "\t\tpopFromStack($ra)\n";		
}

copy (output);